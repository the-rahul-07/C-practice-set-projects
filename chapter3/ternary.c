#include <stdio.h>

int main()
{
    // condition ? expression if ture : expression if false
    int a = 300;
    int b = 120;
    a > b ? printf("a is greater") : printf("b is greater");
    return 0;
}