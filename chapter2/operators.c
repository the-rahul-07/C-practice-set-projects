#include <stdio.h>

int main()
{
    // OPERATORS EXAMPLES AND SOME EXPLANATION
    float a = 5, b = 6;
    int c = 4, e = 3;
    printf("a+b = %f\n", a + b);         // addition operator
    printf("a-b = %f\n", a - b);         // subtraction operator
    printf("a*b = %f\n", a * b);         // multipication operator
    printf("a/b = %f\n", a / b);         // division operator
    printf("c modulas e = %d\n", c % e); // modulas operator --> returns the remainder, used to check the divisibilty

    return 0;
}